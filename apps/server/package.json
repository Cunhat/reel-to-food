{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.9.0",
    "@reel-to-food/auth": "workspace:*",
    "@reel-to-food/db": "workspace:*",
    "@reel-to-food/env": "workspace:*",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.32",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@reel-to-food/config": "workspace:*",
    "@types/node": "^22.20.1",
    "alchemy": "catalog:",
    "tsdown": "^0.22.14",
    "typescript": "^6.0.3",
    "wrangler": "^4.115.0"
  }
}
